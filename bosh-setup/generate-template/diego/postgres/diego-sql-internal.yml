sql_overrides:
  bbs:
    db_connection_string: (( "postgres://diego:" properties.databases.roles.diego.password "@" jobs.postgres_z1.networks.cf1.static_ips.[0] ":5524/diego" ))
    db_driver: postgres
    max_open_connections: 500

jobs: (( merge ))
properties: (( merge ))
